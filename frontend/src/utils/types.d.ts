export interface IUser {
  id: string;
  name: string;
  email: string;
  workload: string;
  role: string;
  wage: number;
  createdAt: string;
}

export interface ISelect {
  label: string;
  value: string;
}